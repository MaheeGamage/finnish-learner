---
status: draft   # draft | active | achieved | superseded
---

<!-- Title comes from the filename, e.g. 001-knowledge-search.md.
     While `status: draft` the goal is being shaped and is fully mutable. Once it goes `active` the `Goal`, `Success criteria`, and `Context` sections are frozen — after that only `status` may change, and a closing `Outcome` is added once when the goal ends (achieved/superseded). -->

## Goal
<!-- The target. Start as a raw idea, then refine it into a clear statement of what you want to achieve. -->

## Success criteria
<!-- How you'll know this goal is met. Checkable, not vague. -->

## Context
<!-- Optional. Given background and what's explicitly out of scope — the framing that
     is part of this goal. Durable constraints/assumptions that can change and outlive
     the goal do NOT go here; they live in the project-level Decisions/Constraints
     layer. -->

## Outcome
<!-- Added once, when status → achieved or superseded. The only part written after the
     freeze. The Re-evaluate verdict: how the outputs measured against the Success
     criteria, with links to them. For superseded, why — and a link to the goal that
     replaces it (when that link convention lands). -->
