---
status: proposed   # proposed | accepted | superseded
date: 2026-06-08
superseded-by:      # optional wikilink, e.g. "[[007-switch-to-postgres]]" — set when this is replaced
---

<!-- Title comes from the filename, numbered + slugged, e.g. 001-use-sqlite.md.
     A decision is IMMUTABLE once `accepted` — it is a point-in-time record. To
     reverse or change it, write a NEW decision and set this one's status to
     `superseded` with a `superseded-by` link (never edit the original's reasoning).
     `proposed` = drafted, awaiting a human's sign-off; `accepted` = signed off and
     in force. Only SIGNIFICANT decisions live here — a choice between real alternatives
     that is costly to reverse. Other things have other homes: requirements → a Goal's
     Success criteria; given bounds → constraints.md; reversible/local calls → the task
     Log (promote one to a record later if it proves load-bearing). -->

## Context
<!-- The forces at play: the situation, the constraints/assumptions that bear on this,
     and the alternatives considered — each with why it was rejected. The rejected
     alternatives are the point: they stop the project (human or AI) re-walking dead ends. -->

## Decision
<!-- The choice that was made. State it plainly. -->

## Consequences
<!-- The resulting trade-offs: what becomes easier, what becomes harder, and any
     follow-up actions. If the decision creates a new standing constraint, add that
     to constraints.md and link back here. -->
