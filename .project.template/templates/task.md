---
status: to-do    # to-do | in-progress | in-review | done
owner: human     # human | ai | both
goal: "[[001-knowledge-search]]"   # which goal this task serves
---

<!-- Title comes from the filename, e.g. survey-existing-tools.md.
     Unlike a goal, a task is mutable — it's a living record, edited as you work. -->

## Description
<!-- What to do and why (the stable intent) on top, then how: the plan and
     sub-tasks as `- [ ]` checkboxes below it. -->

## Done when
<!-- The check that says this task is complete. -->

## Outputs
<!-- What this task produced. One entry per artifact: a link to where it lives in
     its natural home (file path, repo/PR URL, or a [[decision]] / [[knowledge]]
     record) plus a one-line note on what it is. Non-file results (a deploy, a
     metric) can be described in a line. Trivial byproducts can stay in the Log.
     Omit the section when there is nothing worth pointing to. Per-file links live
     here, not in the Overview — the Overview only maps where each *kind* of output
     is saved. -->

## Log
<!-- Chronological, append-only, attributed (`[human]` / `[ai]`): progress notes,
     minor decisions, and steering/review comments — drop a comment in anytime
     ("do it this way, not that") and the doer's response follows in the thread.
     Significant decisions — real alternatives AND costly to reverse — graduate to a
     Decision record; the Log is where they start, lift one out when it proves
     load-bearing. -->